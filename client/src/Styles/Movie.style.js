import styled from "styled-components"

const MovieContainer = styled.div`
  color: #8A8A8A;

  .slideshow {
    width: 350px;
    height: 500px;
    object-fit: cover;
  }
`

export { MovieContainer }